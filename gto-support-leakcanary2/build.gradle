dependencies {
    implementation "com.squareup.leakcanary:leakcanary-android:${deps2.leakcanary2}"

    compileOnly "com.google.firebase:firebase-crashlytics:${deps2.firebase.crashlytics}"
    compileOnly "com.jakewharton.timber:timber:${deps.timber}"
}
